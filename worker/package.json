{
  "name": "our-little-list-delivery",
  "private": true,
  "type": "module",
  "description": "Sends the reminders and notifications the website queues up.",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "cd .. && pnpm run test:unit"
  },
  "devDependencies": {
    "wrangler": "4.137.0"
  }
}
